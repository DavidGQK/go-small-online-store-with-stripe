#!/usr/bin/env bash

#go build -o bookings cmd/web/*.go &&
#./bookings -dbhost=localhost -dbport=5432 -dbname=golang_database -dbuser=postgres -dbpass=12345 -cache=false -production=false
go run ./cmd/web